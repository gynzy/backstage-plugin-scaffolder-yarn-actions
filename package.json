{
  "name": "@gynzy/backstage-plugin-scaffolder-yarn-actions",
  "description": "The yarn-actions module for @backstage/plugin-scaffolder-backend",
  "version": "0.0.2",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/gynzy/backstage-plugin-scaffolder-yarn-actions.git"
  },
  "publishConfig": {
    "access": "public",
    "main": "dist/index.cjs.js",
    "types": "dist/index.d.ts"
  },
  "backstage": {
    "role": "backend-plugin-module"
  },
  "scripts": {
    "start": "backstage-cli package start",
    "build": "backstage-cli package build",
    "lint": "backstage-cli package lint",
    "test": "backstage-cli package test",
    "clean": "backstage-cli package clean",
    "prepack": "backstage-cli package prepack",
    "postpack": "backstage-cli package postpack"
  },
  "dependencies": {
    "@backstage/plugin-scaffolder-backend": "^1.19.0",
    "@backstage/plugin-scaffolder-node": "^0.2.8"
  },
  "devDependencies": {
    "@backstage/backend-common": "^0.19.0",
    "@backstage/cli": "^0.24.0",
    "@types/jest": "^29.2.3"
  },
  "files": [
    "dist"
  ]
}
